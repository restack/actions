{
  "name": "@restack/k8s-manifest-updater",
  "version": "0.0.0",
  "private": true,
  "description": "Update K8s manifests with new images",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --license licenses.txt",
    "test": "jest",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@restack/action-core": "workspace:*",
    "@restack/github-app-client": "workspace:*",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@vercel/ncc": "^0.38.4"
  }
}
